name: BatCave Build and Release

on:
  workflow_dispatch:
    inputs:
      skip-pypi-test:
        type: boolean
  push:
    branches:
      - main
      - feature/*
      - release/*
  pull_request:
    branches:
      - main
      - feature/*
      - release/*

permissions:
  checks: write
  contents: write
  id-token: write
  issues: write
  pull-requests: write

jobs:
  ci-cd:
    uses: arisilon/shared-actions/.github/workflows/python-cicd.yaml@main
    with:
      build-docs: true
      install-test-action: "python -c 'import batcave; print(batcave.__version__)'"
      skip-pypi-test: ${{ inputs.skip-pypi-test || false }}
      update-changelog: true
    secrets: inherit
