version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  jobs:
    pre_build:
      - sphinx-apidoc -f -o docs ../batcave

python:
  install:
    - requirements: docs/requirements.txt
    - path: .

sphinx:
  configuration: docs/conf.py